This folder contains OpenAPI fragments for DeepskyLog APIs.

- `search.yml` — OpenAPI 3.0 fragment documenting `GET /api/search` (parameters, responses, examples).

How to use:
- Merge this fragment into your overall OpenAPI spec or include it with your tooling.
- The fragment assumes the path is mounted at `/api/search`.
